{
  "name": "solution-living-canvas",
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix client ci && npm --prefix client run build && npm --prefix server ci && npm --prefix server run build",
    "start": "cd server && npm start"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0",
    "fluent-ffmpeg": "^2.1.3",
    "phaser": "^3.88.2",
    "sharp": "^0.34.1"
  }
}
